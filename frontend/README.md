# Agent Project Intelligence Frontend

This directory is the project boundary for the React frontend. Frontend source,
tests, and dependency metadata will live here and will consume interfaces
provided by the FastAPI backend in `../backend/`.

Only the project boundary is established so far. Routing, build tooling,
rendering mode, component libraries, and hosting remain open decisions, so a
React scaffold is intentionally not selected in this change.
